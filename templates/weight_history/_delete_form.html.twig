<form method="post" action="{{ path('app_weight_history_delete', {'id': weight_history.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weight_history.id) }}">
    <button class="button is-danger">Delete</button>
</form>
