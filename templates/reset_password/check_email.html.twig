{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
    <section class="section">
        <div class="container mx-auto px-4 sm:px-8">
            <h1 class="text-4xl font-bold mb-4">Password Reset Email Sent</h1>

            <div class="box bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4 text-center">
                <p class="mb-4">
                    If an account matching your email exists, then an email was just sent that contains a link that you can use to reset your password.
                    This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </p>
                <p>
                    If you don't receive an email please check your spam folder or <a href="{{ path('app_forgot_password_request') }}" class="text-blue-500 hover:text-blue-800">try again</a>.
                </p>
            </div>
        </div>
    </section>
{% endblock %}