{% extends 'base.html.twig' %}

{% block title %}Machines{% endblock %}

{% block body %}
    <h1>Machines</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ machine.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ machine.name }}</td>
            </tr>
            <tr>
                <th>Max_capacity</th>
                <td>{{ machine.maxCapacity }}</td>
            </tr>
            <tr>
                <th>Weight_increment</th>
                <td>{{ machine.weightIncrement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_machines_index') }}">back to list</a>

    <a href="{{ path('app_machines_edit', {'id': machine.id}) }}">edit</a>

    {{ include('machines/_delete_form.html.twig') }}
{% endblock %}
