{% extends 'base.html.twig' %}

{% block title %}Machines index{% endblock %}

{% block body %}
    <h1>Machines index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Max_capacity</th>
                <th>Weight_increment</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for machine in machines %}
            <tr>
                <td>{{ machine.id }}</td>
                <td>{{ machine.name }}</td>
                <td>{{ machine.maxCapacity }}</td>
                <td>{{ machine.weightIncrement }}</td>
                <td>
                    <a href="{{ path('app_machines_show', {'id': machine.id}) }}">show</a>
                    <a href="{{ path('app_machines_edit', {'id': machine.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_machines_new') }}">Create new</a>
{% endblock %}
