<form method="post" action="{{ path('app_training_execution_delete', {'id': training_execution.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training_execution.id) }}">
    <button class="button is-danger">Delete</button>
</form>
